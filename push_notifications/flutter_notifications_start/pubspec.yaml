name: meetups
description: Dev meetups is a flutter project to a course of the Alura
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http:
  firebase_core: ^1.23.0
  firebase_messaging: ^13.0.3
  device_info_plus:
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
